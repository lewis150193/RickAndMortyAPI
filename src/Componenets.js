import React from 'react'
export const Form = ({userId, id, title, value,send}) => (
    <>
        <form onClick={send}>
            <input type='text' value={value} onChange={} />
            <input  value={value}/>
            <input />
            <input />

        </form>
     </>
)